version: 2.1

jobs:
  deploy:
    machine:
      image: ubuntu-2204:2023.07.2
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh -o StrictHostKeyChecking=no ubuntu@13.201.80.196 "cd /home/ubuntu/ivend-bench/apps/erpnext"

workflows:
  build-and-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only: develop
